import gzip, pickle
import numpy as np
import csv
import matplotlib.pyplot as plt

with gzip.open("dataset/mnist.pkl.gz") as f:
    train_set, valid_set, test_set = pickle.load(f, encoding="bytes")


numelements = train_set[0].shape[0] # getting the numelements from the size of train_set[0]

randomNum = np.random.randint(low=0, high=numelements, size=1)
pixels = train_set[0][randomNum].reshape((28, 28))
print(randomNum)

plt.imshow(pixels, cmap='gray')
plt.show()
